import { Routes } from '@angular/router';
import { IosTestComponent } from '../components/ios-test/ios-test.component';

export const routes: Routes = [
  {
    path: 'ios-test',
    component: IosTestComponent
  }
];
